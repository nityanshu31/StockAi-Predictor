services:
  - type: web
    name: my-app
    env: python
    buildCommand: |
      curl https://sh.rustup.rs -sSf | sh -s -- -y
      export PATH="$HOME/.cargo/bin:$PATH"
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python app.py  # or your app entry point
